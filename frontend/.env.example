# Copy to .env.local to override. Optional.

# Base URL of the CityPulse backend API (no trailing slash). Read only in
# src/app/useEvents.js; every component goes through that shared hook, never this
# variable directly.
VITE_API_BASE=http://localhost:3000
