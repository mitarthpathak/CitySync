# Copy to .env to override. Both are optional; defaults shown.

# Port the API listens on.
PORT=3000

# true  = serve ONLY events.json, make no network calls (offline demo).
# false = live feeds, with events.json as per-feed fallback.
USE_MOCK=false

# --- LOCAL ---
# WAQI_TOKEN is shared with GLOBE (see docs/CONTRACT.md) - set it once here.
# Get a free token at https://aqicn.org/data-platform/token/
WAQI_TOKEN=

# --- GLOBE ---
# Uses the shared WAQI_TOKEN above. Add globe-only vars under this header.
